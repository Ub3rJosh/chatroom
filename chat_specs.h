/* Author: Joshua Maldonado */

const int PORT = 8080;       // max number of connections to the server at a time
const int MAX_CLIENTS = 4;   // port to connect to
const int MAX = 256;         // max message size
const int NAME_SIZE = 24;    // max size for usernames
const int COMMAND_SIZE = 8;  // max size for commands (ex. @whisper or @exit)
